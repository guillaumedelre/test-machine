<div class="table-responsive">
    <table class="table table-hover table-striped">
        <thead>
        <tr>
            <th>Catégorie</th>
            <th class="text-right">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.name }}</td>
                <td class="text-right">
                    <a href="{{ url('admin_category_edit', {id: category.id}) }}" data-toggle="tooltip" title="Editer" class="btn btn-xs btn-info"><i class="glyphicon glyphicon-edit"></i></a>
                    <a href="{{ url('admin_category_delete', {id: category.id}) }}" data-toggle="tooltip" title="Supprimer" class="btn btn-xs btn-danger delete-link"><i class="glyphicon glyphicon-remove"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>